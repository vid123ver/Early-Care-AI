## Backend environment variables (DO NOT COMMIT your real .env)
#
# MongoDB Atlas connection string:
# - Replace <password> and <cluster-host>
# - Keep the database name as `earlycare` (or change it consistently)
MONGO_URI="mongodb+srv://vidhanverma2311_db_user:<gNTV2AgB7dHQn4sb>@<cluster-host>/earlycare?retryWrites=true&w=majority"

# JWT signing secret (set a long random value in production)
JWT_SECRET="change_me_to_a_long_random_secret_key_at_least_32_bytes"

